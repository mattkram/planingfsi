# Create submeshes

# Create points (ID, type, params)
mesh.addPoint(1, 'rel', [0, 180.0, 1.0])
mesh.addPoint(2, 'rel', [0,   0.0, 1.0])

mesh.movePoints(0.0, 0.5, [1, 2])

meshMembrane = mesh.addSubmesh('membrane')
meshMembrane.addCurve(1, 2, Nel=50, arcLen=2.5)

mesh.fixPoints([1, 2])
